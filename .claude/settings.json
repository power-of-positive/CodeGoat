{
  "permissions": {
    "deny": [
      "Update(.claude/settings.json)",
      "Update(settings.json)",
      "Update(tsconfig.json)",
      "Update(scripts/claude-stop-hook.ts)",
      "Update(claude-stop-hook.ts)",
      "Update(settings.json)",
      "Update(ui/jest.config.ts)",
      "Update(../settings-precommit.json)",
      "Update(settings-precommit.json)",
      "Update(eslint.shared.mjs)",
      "Update(settings-precommit.json)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|MultiEdit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'file_path=$(echo \"$CLAUDE_TOOL_INPUT\" | jq -r \".file_path // empty\"); if [[ -n \"$file_path\" && \"$file_path\" =~ \\.(ts|tsx|js|jsx)$ ]]; then cd /workspace/frontend && echo \"🎨 Auto-formatting $file_path\" && npx prettier --write \"$file_path\" && echo \"🔧 Running ESLint fixes\" && npm run lint:fix -- \"$file_path\" --quiet && echo \"🔍 TypeScript type checking\" && npx tsc --noEmit --skipLibCheck && echo \"✅ All checks passed for $file_path\"; fi'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Stop hook ran at: ' $(date) >> /tmp/claude-stop-hook.log && echo 'Stop hook triggered!'"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"I ran\""
          }
        ]
      }
    ]
  }
}
